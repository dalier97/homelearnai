{
  "name": "site",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "wrangler pages deploy . --project-name=homelearnai",
    "dev": "wrangler pages dev .",
    "preview": "wrangler pages deploy . --project-name=homelearnai --compatibility-date=2024-01-15"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "wrangler": "^4.35.0"
  }
}
